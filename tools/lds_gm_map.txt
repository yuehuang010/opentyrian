# lds_gm_map.txt - OPL2 voice fingerprint -> General MIDI program map
#
# Used by tools/lds_to_midi.c to decide what GM instrument (0-127) a decoded
# OPL2 voice should sound like when the MIDI is rendered through a
# soundfont/synth (e.g. fluidsynth), and optionally to transpose it.
#
# Each fingerprint is an 8-hex-digit FNV-1a hash over an OPL channel's
# timbre-defining registers (modulator + carrier {0x20,0x40,0x60,0x80,0xE0}
# offsets, plus the 0xC0+ch feedback/connection register) at the moment a
# note-on happens. Different instruments in the same track produce different
# fingerprints; the same instrument reused later reproduces the same one.
#
# Run `tools/lds_to_midi <data_dir> <out_dir> <song>` and read the resulting
# hdmusic_NN.patches report to see which fingerprints appear, on which OPL
# channels, how often, and whether they're currently mapped. Copy the
# fingerprints you care about into this file with your chosen GM program.
#
# Format (whitespace separated, '#' comments allowed):
#   <8-hex-fingerprint>  <gm_program 0-127>  [semitone_transpose]  [volume 0-127]
#
# volume is a per-instrument mix level (MIDI CC7, default 100) emitted when
# the fingerprint takes over a channel; use it to push leads forward and
# tuck accompaniment back without touching the decoded dynamics.
#
# gm_program follows the General MIDI Level 1 program list (0 = Acoustic
# Grand Piano, 24 = Nylon Guitar, 33 = Electric Bass (finger), 56 = Trumpet,
# 61 = Brass Section, 80 = Lead 1 (square), 81 = Lead 2 (sawtooth), etc.)
#
# semitone_transpose is optional and defaults to 0; it's applied to every
# note emitted on a channel while that fingerprint is active (useful if an
# OPL voice's synthesized octave doesn't match its GM counterpart's natural
# register).
#
# Example (not applied until you uncomment/edit with real fingerprints from
# a .patches report):
# 1a2b3c4d  81  0

# ----------------------------------------------------------------------
# Track 30 (title theme) - third pass: the first-draft assignments won the
# listening test over the "remix" pass, so this is the draft verbatim with
# two ear-driven fixes:
#   - 4b52fb40 Trumpet -> French Horn: FluidR3's trumpet at max velocity
#     plays an overblown sample layer that reads as distortion (~0:35+).
#   - 15a839b2 tinkle bell silenced (vol 0): rang 3x/phrase with no audible
#     counterpart in the OPL original.
# gate is in sequencer ticks, 1 tick ~ 14.4 ms.
# ----------------------------------------------------------------------
c5eedaea  48   # ch2,4  4 long low notes (26..38, ~11s)  -> String Ensemble pad
f467d225  61   # ch0,1  mid riff (57..70, ~140ms)        -> Brass Section
1cd3af4f  61   # ch0,1,4 very short (23ms) stabs (V3)    -> Brass Section (these are melody ornaments on the melody's own channels; pizzicato made them sound like a stray instrument)
74a8c820  61   # ch0,1  main melody voice (43..81)       -> Brass Section
4a4a0c2e  46   # ch3,4  busiest arp/accomp (1254 notes)  -> Orchestral Harp
4ffe5d4b  42   # ch2..6 mid-low sustained (38..58)       -> Cello
e181198e  52   # ch4,5  long soaring lead (48..93, ~1s)  -> Choir Aahs
13fecaf5  38   # ch2    driving mid-low rhythm line      -> Synth Bass 1
bf6c72e9  38   # ch5,6  bass line (26..53)               -> Synth Bass 1
efa39919  47  0  0  # ch6,7  fixed note 36 hits (V10)    -> REMOVED (vol 0; was Timpani)
e647c759  43   # ch1    4 very long low notes (37)       -> Contrabass
690de1b2  115  # ch8    fixed note 85 ticking            -> Woodblock
8251725a  12  0  0  # ch7  4751-note 37ms arpeggio (V13)  -> REMOVED (vol 0; no instrument sat right for this texture)
4b52fb40  29   # ch0,1  second melody (45..81) (V14)     -> Overdriven Guitar (per ear test; trumpet distorted, horn washed, brass muffled, synth brass meh)
9e5b1e21  45   # ch5,6  high ostinato (69..77)           -> Pizzicato Strings
7d7e14af  46   # ch4,5  fast high runs/fills             -> Orchestral Harp
6e979d58  46   # ch4    fast high runs/fills             -> Orchestral Harp
3bdee0e9  46   # ch4    fast high runs/fills             -> Orchestral Harp
7fda9b3c  42   # ch3    mid-low rhythm (38..57)          -> Cello
ff66498c  12  0  85  # ch6  fast repeated mid notes (V19) -> Marimba (pizzicato mushed the 37ms same-note repeats; mallets keep them distinct)
0d5f4d92  38   # ch2    bass (26..45)                    -> Synth Bass 1
b7d5240c  89   # ch8    very long high drone (84)        -> Pad 2 (warm)
6e6c1c52  46   # ch0,4  short fills                      -> Orchestral Harp
6898f39b  46   # ch0,1,4 short fills                     -> Orchestral Harp
38d2a11c  46   # ch0,4  short fills                      -> Orchestral Harp
7efe0866  46   # ch0,4  short fills                      -> Orchestral Harp
d2f0af9d  46   # ch0,4  short fills                      -> Orchestral Harp
f033864b  46   # ch4,5  short fills                      -> Orchestral Harp
00d2b08d  48   # ch5,6  mid sustained (45..61)           -> String Ensemble
15a839b2  112 0 0  # ch8  fixed note 84 ticking          -> silenced (bell with no OPL-audible counterpart)
d877be97  43   # ch5,6  low sustained (25..37)           -> Contrabass
